# NUMDEV
Lightweight Chrome extension that detects 10-digit phone numbers in CRM pages and converts them into clickable tel: links, enabling native browser/device call handoff without a backend or mobile app.
